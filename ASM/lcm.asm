; Question : Given 8 bit numbers a and b find their LCM and HCF

ASSUME CS:CODE, DS:DATA
DATA SEGMENT
	a 	DB 04H
	b 	DB 08H
	lcm 	DB ?
	hcf 	DB ?
DATA ENDS

CODE SEGMENT
	PRINT:
		MOV AH, 2
		MOV DL, lcm
		ADD DL, 30H
		INT 21H
		MOV DL, hcf
		ADD DL, 30H
		INT 21H
		RET

	START:
		MOV AX, DATA
		MOV DS, AX

		MOV AL, a
		MOV BL, b 			

	BACK:
		MOV AH, 00H
		ADD BL, 00H  			
		JNZ GET_HCF
		MOV hcf, AL
		JMP FINISH

	GET_HCF:
		DIV BL
		MOV AL, BL
		MOV BL, AH
		JMP BACK

	FINISH:
		MOV AL, a
		MOV BL, b
		MUL BL
		MOV BL, hcf
		DIV BL
		MOV lcm, AL
		CALL PRINT
		MOV AH, 4CH
		INT 21H

CODE ENDS
END START


COMMENT @@@
MEMORY:

0800   04
0801   08
0802   ??
0803   ??


CODE:
0300: MOV AL, [0800]
0302: MOV BL, [0801]
0304: MOV AH, 00H
0306: ADD BL, 00H
0308: JNZ 030E
030A: MOV [0803], AL
030C: JMP 0316
030E: DIV BL
0310: MOV AL, BL
0312: MOV BL, AH
0314: JMP 0304
0316: MOV AL, [0800]
0318: MOV BL, [0801]
031A: MUL BL
031C: MOV BL, [0803]
031E: DIV BL
0320: MOV [0802], AL
0322: HLT
0324: CODE ENDS
@@@